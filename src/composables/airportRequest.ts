// import connection from './websocket';

// export default function airportRequest(value: string) {
// 	if (value.length > 3) {
// 		const req = { type: 'airportRequest', data: `${value.toUpperCase()}` };
// 		connection.send(JSON.stringify(req));
// 	}
// }
